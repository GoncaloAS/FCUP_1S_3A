## Construtor - Peças ##


<link rel="stylesheet" href="tictactoe.css" type="text/css"> 

<script>

window._update = function() {

TicTacToe.prototype.content = new Array(9);
TicTacToe.prototype.board = new Array(9);
TicTacToe.prototype.current = "X";

function TicTacToe(id) {

    var parent = document.getElementById(id);
    var board  = document.createElement("div");

    board.className = "board";
    parent.appendChild(board);

    for(let i=0; i<9; i++) {
	  let cell = document.createElement("div");
	  cell.className = "cell"
	  board.appendChild(cell);

	  cell.onclick = () => this.play(i);
	
	  this.board[i] = cell;
    }
    
}

TicTacToe.prototype.play = function(pos) {
    this.content[pos] = this.current;
    this.board[pos].innerHTML = this.current;
    
    this.current = (this.current == "X" ? "O" : "X");
}

var ticTacToe = new TicTacToe("base");

}

</script>



<div class="col75p terminal example cozy">constructor(id) {    // ...
   for(<code>let i</code>=0; i<9; i++) {
       let cell = document.createElement("div");
       cell.className = "cell";
       board.appendChild(cell);
 
       <span class=focus>cell.onclick = () => this.play(i);</span>
	  
       this.board[i] = cell;
   }   
}</div>
<div class=col25p >

<div id=base style="width: 100%;  transform: scale(.70,.70);"> </div>

</div>


* Propriedade `onclick` guarda *callback* de cada célula
* Função anónima invocada quando botão é pressionado 
* Variável de controlo de ciclo definida com `let` ...
* ... retém o valor da iteração em que foi definida
