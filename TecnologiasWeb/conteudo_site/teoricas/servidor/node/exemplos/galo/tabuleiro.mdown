## Tabuleiro ##


<div class="terminal example cozy">
let current = 'X';
let board = undefined;

function initBoard() {
    board = new Array(9);
    
    for(let b=0; b<9; b++) {
        board[b] = '&nbsp;';
    }
}

function play() {
   let piece = current;
            
   current = (current == 'X' ? 'O' : 'X'); 
   board[message.pos] = piece;
}

initBoard();
</div>

* Processamento do tabuleiro de jogo